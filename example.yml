database:
    driverClass: com.mysql.cj.jdbc.Driver
    user: prenosdb
    password: 
    url: jdbc:mysql://mysql4.gear.host:3306/prenosdb?useSSL=false
    properties:
        charSet: UTF-8
        hibernate.dialect: org.hibernate.dialect.MySQLDialect
        hibernate.hbm2ddl.auto: create
